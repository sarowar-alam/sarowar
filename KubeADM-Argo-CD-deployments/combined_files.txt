app-bluegreen\rollout.yaml
==========================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: nginx-bluegreen
spec:
  replicas: 3
  strategy:
    blueGreen:
      activeService: nginx-bluegreen-active
      previewService: nginx-bluegreen-preview
      autoPromotionEnabled: false
  selector:
    matchLabels:
      app: nginx-bluegreen
  template:
    metadata:
      labels:
        app: nginx-bluegreen
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports:
            - containerPort: 80



app-bluegreen\service.yaml
==========================
apiVersion: v1
kind: Service
metadata:
  name: nginx-bluegreen-active
spec:
  selector:
    app: nginx-bluegreen
  ports:
    - port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-bluegreen-preview
spec:
  selector:
    app: nginx-bluegreen
  ports:
    - port: 80



app-canary\rollout.yaml
=======================
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: nginx-canary
spec:
  replicas: 5
  strategy:
    canary:
      steps:
        - setWeight: 20
        - pause: { duration: 30s }
        - setWeight: 50
        - pause: { duration: 1m }
        - setWeight: 100
  selector:
    matchLabels:
      app: nginx-canary
  template:
    metadata:
      labels:
        app: nginx-canary
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports:
            - containerPort: 80



app-canary\service.yaml
=======================
apiVersion: v1
kind: Service
metadata:
  name: nginx-canary-svc
spec:
  selector:
    app: nginx-canary
  ports:
    - port: 80



app-rolling\deployment.yaml
===========================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-rolling
  labels:
    app: nginx-rolling
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-rolling
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: nginx-rolling
    spec:
    containers:
      - name: nginx
        image: nginx:1.27
        ports:
          - containerPort: 80



app-rolling\service.yaml
========================
apiVersion: v1
kind: Service
metadata:
  name: nginx-rolling-svc
spec:
  selector:
    app: nginx-rolling
  ports:
    - port: 80
      targetPort: 80




