apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: bmi-health-tracker
  labels:
    app: bmi-backend
data:
  NODE_ENV: "production"
  PORT: "3000"
  FRONTEND_URL: "http://your-domain.com"  # CHANGE THIS to your actual domain or LoadBalancer IP
