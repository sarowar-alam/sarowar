apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: bmi-health-tracker
  labels:
    app: bmi-backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bmi-backend
