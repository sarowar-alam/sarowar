name: CI - Code Quality and Security

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci
        working-directory: terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS

      - name: Run security audit
        run: npm audit --audit-level moderate
        working-directory: terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS

      - name: Check Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS/Dockerfile

  terraform-validation:
    name: Terraform Validation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Format
        run: |
          cd terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS/terraform
          terraform fmt -check -recursive

      - name: Terraform Validate
        run: |
          cd terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS/terraform
          terraform validate

      - name: Terraform Security Scan
        uses: aquasecurity/tfsec-action@master
        with:
          directory: terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS/terraform

  build-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          cd terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS
          docker build -t cpu-load-test-ci:${{ github.sha }} .

      - name: Test Docker image
        run: |
          cd terraform-ecs-module-target-auto-scale-dynamic-json-Node-JS
          docker run --rm \
            -e NODE_ENV=test \
            cpu-load-test-ci:${{ github.sha }} \
            node -e "console.log('Docker image test passed'); process.exit(0);"
